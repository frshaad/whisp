import { Inter, Pacifico } from 'next/font/google';

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
});

export const pacifico = Pacifico({
  subsets: ['latin'],
  weight: ['400'],
  display: 'swap',
});
